apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myclaim-mysql
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage